# Todogram 기능 명세서 (사용자 관점)

- 프로젝트: `todo-list-pwa`
- 문서 목적: 실제 사용자가 앱에서 경험하는 모든 기능(UI 작동 방식 포함) 정의
- 기준 시점: 2026-03-04
- 분석 방식: 코드베이스 전체(`src/App.vue`, `src/components`, `api/*`) + 빌드 실행 검증(`npm run build`)

## 1) 제품 개요

Todogram은 계정 기반 Todo 관리 앱으로, 리스트/캘린더 보기, 상세 편집, 댓글, 라벨, 푸시 알림(PWA Web Push), 사용자 설정, 개선요청(피드백) 기능을 제공한다.

핵심 사용자 가치:
- 일정의 상태(대기/진행중/완료)와 마감일 기준으로 체계적으로 관리
- 드래그 정렬과 필터링으로 빠른 우선순위 조정
- 일정 상세/댓글/라벨로 협업 전 개인 문맥 기록 강화
- PWA 설치 + 푸시 알림으로 데스크톱/모바일에서 상시 접근

## 2) 화면 구조

- 인증 화면(로그인/회원가입 탭)
- 메인 화면
- 리스트 뷰
- 캘린더 뷰
- 일정 추가 모달
- 일정 상세 모달(조회/편집/삭제/댓글)
- 라벨 설정 모달(라벨 생성/수정)
- 앱 설정 모달(데스크톱)
- 모바일 메뉴 모달 + 상세 설정 패널
- 프로필 모달(조회/편집)
- 개선요청 작성 모달
- 개선요청 검토 모달(관리자)
- 커스텀 확인 모달(삭제 재확인)

## 3) 인증 및 세션 기능

### 3.1 로그인/회원가입
- 이메일 + 비밀번호 기반 인증
- 회원가입 시 사용자명 필수(2~24자), 비밀번호 최소 8자
- 로그인/회원가입 실패 시 에러 메시지 표시
- 인증 성공 시 세션 쿠키 발급

### 3.2 로그아웃
- 로그아웃 버튼(데스크톱/모바일 메뉴) 제공
- 로그아웃 시 로컬 UI 상태(투두/라벨/모달/초안 등) 초기화
- 푸시 구독이 있는 경우 서버/브라우저 구독 정리 시도

### 3.3 세션 유지
- 앱 시작 시 `GET /api/auth`로 세션 사용자 복원
- 세션 만료/미인증 시 인증 화면으로 유지

## 4) 메인 공통 UI 동작

- 상단에서 리스트/캘린더 보기 전환
- 검색 입력 실시간 반영
- 상태 필터(전체/대기/진행중/완료)
- 라벨 필터(전체 라벨 + 등록 라벨)
- 요약 수치(완료율/오늘 일정 수 등) 표시
- 다국어 지원: 한국어/영어/중국어/일본어
- 테마 지원: 라이트/다크
- 모달 열림 시 배경 스크롤 잠금

## 5) 리스트 뷰 기능

### 5.1 일정 목록 표시
- 정렬 기준: 사용자별 `position ASC`, 동일 순위는 `created_at DESC`
- 항목 표시 정보:
- 제목
- 내용 미리보기
- 마감일시
- 라벨(멀티 라벨 요약: 첫 라벨 + n)
- 상태
- 댓글 개수
- 장소

### 5.2 상태 변경
- 상태 셀렉트에서 `대기/진행중/완료` 직접 선택 가능
- 완료로 변경 시 해당 항목을 목록 하단으로 이동
- 서버 반영 실패 시 UI 롤백

### 5.3 제목 드래그 정렬
- 제목 버튼을 드래그 핸들로 사용
- 드래그 종료 시 `order`를 서버에 저장
- 검색 중 또는 처리중(`busy`)일 때 드래그 비활성화
- 드래그 직후 오클릭으로 상세가 열리는 문제 방지 로직 포함

### 5.4 상세 열기
- 제목 클릭 시 일정 상세 모달 오픈

## 6) 캘린더 뷰 기능

### 6.1 월간 캘린더
- 월 이동(이전/다음)
- 오늘로 이동 버튼
- 각 날짜 셀에 해당 날짜 일정 표시
- 날짜별 일정이 많을 경우 `더보기`로 날짜 상세 리스트 열기

### 6.2 보조 리스트
- 특정 날짜의 일정 리스트 모달
- 마감일 없는 일정 리스트 모달
- 리스트 항목 클릭 시 상세 모달로 이동

## 7) 일정 추가 기능

### 7.1 모달 진입
- `일정 추가` 버튼 클릭 시 오픈
- 기본값 자동 반영:
- 미완료 자동 이월: 설정 기본값
- 기본 상태: 설정 기본값
- 마감일시: 설정된 오프셋 기준 현재 시각 + N분 (기본 10분)

### 7.2 입력 항목
- 제목(필수)
- 메모/내용
- 마감일시(DateTimePicker)
- 장소
- 라벨(멀티 선택)
- 미완료 자동 이월 토글

### 7.3 라벨 멀티 선택 UX
- 드롭다운에서 다중 선택/해제
- 선택 개수 요약 문구 표시
- 선택 칩 표시
- 라벨이 10개 초과 시 검색 입력 노출
- 드롭다운 외부 클릭 시 닫힘
- 드롭다운은 고정 레이어(`position: fixed`)로 표시되어 레이아웃 밀림 방지

### 7.4 저장
- 저장 성공 시 목록 상단에 새 일정 추가
- 모달 닫힘 + 입력값 초기화
- 실패 시 에러 메시지 표시

## 8) 일정 상세 기능

### 8.1 조회 모드
- 제목/내용/마감일시/라벨/장소/생성일/이월 설정 표시
- 상태 셀렉트(대기/진행중/완료) 즉시 변경 가능

### 8.2 편집 모드
- 제목/내용/마감일시/장소/라벨/이월 옵션 수정
- 저장/취소 제공
- 실패 시 기존 값으로 롤백

### 8.3 삭제
- 삭제 버튼 클릭 시 커스텀 확인 모달 표시
- 확인 시 삭제, 취소 시 유지

### 8.4 댓글 기능
- 댓글 작성
- 댓글 수정(인라인 편집)
- 댓글 삭제(커스텀 확인 모달)
- 댓글 수 표시

## 9) 라벨 관리 기능

### 9.1 라벨 목록/생성
- 라벨 설정 모달에서 라벨 생성
- 이름 + 색상 입력
- 추천 색상 10개 제안 + 리롤 가능

### 9.2 라벨 수정
- 기존 라벨 이름/색상 수정
- 수정 시 Todo에 연결된 라벨명/색상 동기 반영

### 9.3 라벨 데이터 모델
- 단일 라벨 호환 필드 + 멀티 라벨 배열 필드 동시 관리
- `labelText/labelColor` + `labelTexts/labelColors`

## 10) 설정 기능 (데스크톱/모바일)

### 10.1 테마/언어
- 라이트/다크 전환
- 언어 선택(ko/en/zh/ja)
- 즉시 반영 + 로컬 저장

### 10.2 일정 기본 설정
- 미완료 자동 이월 기본값
- 새 일정 기본 상태(대기/진행중)
- 새 일정 기본 마감시간 오프셋(현재, 5/10/15/30/45/60분)

### 10.3 알림 설정
- 푸시 알림 켜기/끄기
- 브라우저 권한/서버 설정 상태에 따른 비활성 처리
- 상태 설명/차단 사유 표시

### 10.4 모바일 설정 정보 구조
- 일반/지원 섹션 카드형 메뉴
- 상세 패널에서 알림/디스플레이/일정/지원 설정 분리

## 11) 프로필 기능

- 현재 사용자명/이메일/아바타 표시
- 편집 모드 진입 후 사용자명/아바타 수정 가능
- 사용자명 중복 확인 버튼 제공
- 이미지 파일 업로드(용량 제한) 또는 URL/데이터 URL 지원
- 이미지 제거 기능

## 12) 개선요청(피드백) 기능

### 12.1 일반 사용자
- 개선요청 작성(제목/내용)
- 제출 성공 시 모달 닫힘

### 12.2 관리자(특정 이메일)
- 개선요청 목록 조회
- 항목 삭제 가능

## 13) 알림 및 PWA 기능

### 13.1 PWA
- 서비스워커 등록
- 설치 가능한 웹앱으로 동작
- 푸시 수신 시 알림 표시, 클릭 시 앱 포커스/오픈

### 13.2 30분 전 리마인더
- 서버 크론 API(`/api/notifications/reminders`)에서 마감 25~35분 전 일정 스캔
- 중복 발송 방지 로그 테이블(`todo_reminder_logs`) 사용
- 만료된 구독 endpoint 자동 정리(404/410)

## 14) 자동 이월(rollover) 동작

- `GET /api/todos` 호출 시점에 자동 이월 조건 검사/실행
- 조건:
- 완료되지 않음
- `rollover_enabled = true`
- `due_at < NOW()`
- 지난 일수만큼 다음 날로 누적 이동

## 15) 확인 모달(삭제 재확인)

- 브라우저 기본 `confirm` 미사용
- 앱 내 커스텀 모달 사용
- 대상: 일정 삭제, 댓글 삭제
- 오버레이/레이어 우선순위 조정 완료(상세 모달 위에 표시)

## 16) 검색/필터링 규칙

- 검색 대상: 제목, 내용, 장소, 라벨(멀티 라벨 포함)
- 상태 필터 + 라벨 필터 동시 적용
- 필터 결과 집합 기준으로 드래그 정렬 시 전체 배열에 매핑 저장

## 17) 입력 검증 및 제한

- 제목: 필수, 최대 120자
- 내용: 최대 4000자
- 장소: 최대 160자
- 라벨명: 최대 32자
- 라벨색: `#RRGGBB`
- 비밀번호: 최소 8자
- 사용자명: 2~24자

## 18) 로컬 저장(브라우저)

- 테마(`todo-theme`)
- 언어(`todo-language`)
- 기본 이월 옵션(`todo-default-rollover`)
- 새 일정 기본 상태(`todo-default-new-status`)
- 새 일정 기본 마감 오프셋(`todo-default-due-offset-minutes`)
- 뷰 상태(사용자별 key prefix 기반)

## 19) 백엔드 API 명세 요약

### 19.1 인증
- `GET /api/auth`: 현재 사용자 조회
- `POST /api/auth`: `signup`, `login`, `check-username`
- `PATCH /api/auth`: 사용자명/아바타 수정
- `DELETE /api/auth`: 로그아웃

### 19.2 Todo
- `GET /api/todos`: 목록 + 댓글 포함 반환 (자동 이월 수행)
- `POST /api/todos`: 일정 생성
- `PATCH /api/todos`: 일정 수정/상태 변경/정렬 저장
- `DELETE /api/todos?id=...`: 단건 삭제
- `DELETE /api/todos?done=true`: 완료 항목 일괄 삭제

### 19.3 댓글
- `POST /api/comments`: 생성
- `PATCH /api/comments`: 수정
- `DELETE /api/comments?id=...`: 삭제

### 19.4 라벨
- `GET /api/labels`: 목록
- `POST /api/labels`: 생성(동일 이름이면 색상 업데이트)
- `PATCH /api/labels`: 수정 + 연결된 Todo 라벨 데이터 동기화

### 19.5 개선요청
- `POST /api/improvements`: 작성
- `GET /api/improvements`: 관리자 조회
- `DELETE /api/improvements?id=...`: 관리자 삭제

### 19.6 알림
- `GET /api/notifications/subscriptions`: 푸시 설정 여부
- `POST /api/notifications/subscriptions`: 구독 등록/갱신
- `DELETE /api/notifications/subscriptions`: 구독 해제
- `GET /api/notifications/reminders`: 크론 전용 리마인더 발송

## 20) 사용자 시나리오(대표 플로우)

### 20.1 신규 사용자
- 회원가입 -> 로그인 -> 일정 추가 -> 상태 변경 -> 상세에서 댓글 작성 -> 설정 조정

### 20.2 일정 운영 사용자
- 리스트에서 검색/필터 -> 제목 드래그 정렬 -> 캘린더로 월별 확인 -> 날짜별 상세 확인

### 20.3 알림 사용자
- 설정에서 알림 허용 -> 브라우저 권한 승인 -> 마감 30분 전 푸시 수신 -> 클릭 후 앱 복귀

## 21) 알려진 운영 포인트

- 빌드 경고: JS 번들 일부 청크가 500KB 초과(기능상 문제는 없음)
- 푸시 알림은 서버 VAPID/CRON 환경변수와 브라우저 권한이 모두 필요
- 관리자 기능은 지정된 이메일과 일치해야 노출/동작

---

문서 버전: `v1.0`  
작성 위치: `todo-list-pwa/TODO_APP_FUNCTIONAL_SPEC.md`
